import { DynamicDirDirective } from './dynamic-dir.directive';

describe('DynamicDirDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDirDirective();
    expect(directive).toBeTruthy();
  });
});
